;; Zettelkasten notes
(leaf org-roam
  :custom* ((org-roam-directory (expand-file-name "zettelkasten" org-directory)))) ; Org roam directory

(leaf orgtbl-aggregate) ; Org table aggregations

(provide 'my-spec-note)
